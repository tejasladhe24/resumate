# `@workspace/mongodb`

Shared MongoDB client and schema package.

## Exports

- `createClient(url)` -> `MongoClient`
- `SUMMARIES_COLLECTION`
- `PARTICIPANTS_ANALYTICS_COLLECTION`
- Zod schemas/types from `src/schema.ts`

## Purpose

Provides one place for Mongo connection setup and common collection contracts.
